export const environment = {
  production: false,
  // PUBLIC_INTERFACE
  // For future integration with a backend API, provide base URL via environment variable.
  /** This is the public config accessor for base API URL. */
  apiBaseUrl: '',
};
